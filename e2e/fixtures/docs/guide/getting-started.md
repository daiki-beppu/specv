# Getting Started

ネストされたディレクトリのドキュメントです。

## 確認ポイント

1. 深い階層のファイルが正しく表示されている
2. サイドバーのツリーで `docs > guide > getting-started.md` が選択状態になっている

## ナビゲーション

- [README.md に戻る](../../README.md) — `../../` で 2 階層上に遷移
- [linked.md に戻る](../linked.md) — `../` で 1 階層上に遷移

## コードブロック（ネストされたページでも動作確認）

```json
{
  "name": "specv",
  "version": "0.2.0",
  "description": "Local Markdown preview"
}
```

コピーボタンが表示されることを確認してください。
